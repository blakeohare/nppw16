class WaterPopAutomation {
	field counter = 0;
	field sprite;

	constructor(sprite) {
		this.sprite = sprite;
	}
		
	function doStuff(scene) {
		this.counter++;
		if (this.counter >= 15) {
			this.sprite.dead = true;
		}
	}
}
