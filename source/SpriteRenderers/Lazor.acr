import Graphics2D;

function SPRITE_renderLazor(sprite, scene, offsetX, offsetY, arc) {
	x = offsetX + sprite.x - 8;
	y = offsetY + sprite.y - 2;
	Graphics2D.Draw.rectangle(x, y, 16, 4, 255, 255, 0);
}
