function SPRITE_renderFireball(sprite, scene, offsetX, offsetY, arc) {
	left = sprite.x - 8 + offsetX;
	top = sprite.y - 8 + offsetY;
	
	img = ImageLibrary.get('sprites/lava_monster_projectile.png');
	
	img.draw(left, top);
}
